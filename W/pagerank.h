//  pagerank.h//  Assignment 2 COMP1927//  3 June 2016//  Created by Winnie Zheng && Vivian Bakiris#ifndef PAGERANK_H#define PAGERANK_H#include <stdio.h>#include <stdlib.h>#include "prlist.h"#include "graph.h"//called in mainvoid PageRank(double d, double diffPR, int maxIter);//called in by pageRank function aboveDLListPR getCollectionPR(); // reads in collection.txt and returns a list of URLS. This function will read the urls from collection.txt (by calling getList from List.c) and store the URLS in list lint numUrls(); //returns number of urls in collectionGraph getGraph(); //creates a new graph by calling Graphnew in graph.h - adds in nV from listLength function#endif