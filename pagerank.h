#ifndef PAGERANK_H#define PAGERANK_H#include <stdio.h>#include <stdlib.h>#include "list.h"#include "graph.h"List getCollection(List l, FILE *f); // reads in collection.txt and returns a list of URLS. This function will read the urls from collection.txt (by calling getList from List.c) and store the URLS in list lList getConnectedUrls(List urls, FILE *f);Graph getGraph(List l, int nV); //creates a new graph by calling Graphnew in graph.h - adds in nV from listLength functionvoid PageRank(float d, float diffPR, int maxIter);#endif